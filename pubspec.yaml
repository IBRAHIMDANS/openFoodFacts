name: openfoodfacts
description: Open Food Facts.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_svg: ^0.19.3
  flutter_bloc: ^7.0.0
  appwrite: ^0.4.0
  dio: ^3.0.10
  share: ^2.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/res/drawables/
    - assets/res/fonts/
    - assets/res/svg/

  fonts:
    - family: AppIcons
      fonts:
        - asset: assets/res/fonts/app_icons.ttf
